apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: enquiry-role
  namespace: default
rules:
- apiGroups: ["hotel.com"]
  resources: ["enquiries"]
  verbs: ["get", "list", "create", "update", "delete"]
